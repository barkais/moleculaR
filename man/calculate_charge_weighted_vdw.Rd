% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_wVDW_steRimol.R
\name{calculate_charge_weighted_vdw}
\alias{calculate_charge_weighted_vdw}
\title{Calculate Charge-Weighted Van der Waals Radius}
\usage{
calculate_charge_weighted_vdw(
  xyz_data,
  atom_index,
  min_distance = 1,
  max_distance = 5,
  damping_factor = 0.7,
  bonds,
  charges
)
}
\arguments{
\item{xyz_data}{Data frame containing atomic coordinates with columns: index, x, y, z}

\item{atom_index}{Integer specifying the index of the atom to analyze}

\item{min_distance}{Numeric minimum distance threshold in Angstroms (default: 1.0)}

\item{max_distance}{Numeric maximum distance threshold in Angstroms (default: 5.0)}

\item{damping_factor}{Numeric factor to moderate charge-based weighting (default: 0.7)}

\item{bonds}{Data frame containing bonding information with columns V1 and V2}

\item{charges}{List of paths to charge model files (NBO, Hirshfeld, CM5)}
}
\value{
Numeric value of the charge-weighted van der Waals radius in Angstroms
}
\description{
Calculates a charge-weighted van der Waals radius using partial atomic charges
from available charge models (NBO, Hirshfeld, CM5). The weighting is modulated
by a damping factor to prevent extreme variations.
}
\details{
The function uses available charge models to weight the contribution of each atom
to the non-bonded distance. The weighting is centered around 0.5 and dampened to
prevent extreme variations. If no charge files are available, falls back to the
unweighted calculation.
}
